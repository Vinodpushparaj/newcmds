#to login in a webpage with username and password
curl --user name:password httpd://webpage.com -v

wget --http-user=user --http-password=password

#to search for a content
curl --user name:password https://www.webpage.com/ | grep whatEverIdentifiesTheLink

cd directory_name

mv source destination
